export type RootStackParamList = {
    index: undefined; // no props expected
    test: undefined; // no props expected
    priceRes: { conversionPrice: string, translateType: string, currentType: string };
}; 